describe("init", function() {
	beforeEach(function() {
		var body = document.getElementsByTagName('body')[0];

		var canvas = document.createElement('canvas');
		canvas.id = 'bridge';
		canvas.width = 300;
		canvas.height = 300;
		body.appendChild(canvas);
	});

	it("should fetch references", function() {
	});
});
